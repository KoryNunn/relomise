{
  "name": "relomise",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "lint": "standardx"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tape": "^4.10.1"
  },
  "devDependencies": {
    "standardx": "^3.0.1"
  },
  "eslintConfig": {
    "rules": {
      "semi": ["error", "always"],
      "indent": ["error", 4],
      "id-match": ["error", "(?!^(?:err|req|res)$)^.*$"]
    }
  }
}
